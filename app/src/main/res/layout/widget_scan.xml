<?xml version="1.0" encoding="utf-8"?>
<!-- Icon-only 1×1: transparent host — brand circle sits on the wallpaper. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_scan_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:contentDescription="@string/widget_scan_description">

    <FrameLayout
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_widget_icon">

        <ImageView
            android:id="@+id/widget_scan_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/widget_action_scan"
            android:importantForAccessibility="no"
            android:padding="13dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_glyph_scan" />
    </FrameLayout>
</FrameLayout>
