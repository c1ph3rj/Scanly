# Sprint 1

Sprint 1 established the application shell.

Historical highlights:

- package boundaries for `core`, `data`, `domain`, `feature`, `di`, and `navigation`
- Hilt-based dependency injection
- a stable navigation shell for the main product routes
- placeholder destinations that later became the feature entry points

This record is kept for traceability only; current architecture notes live in `implementation.md`.
